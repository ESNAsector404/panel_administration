# Copie : `cp .env.example .env` (valeurs par défaut prêtes pour le dev local).

# Unique URL de l'API : utilisée par le front (apiFetch + SSE) et par le proxy
# de dev Vite, qui en dérive sa cible. URL relative ('/api') = same-origin direct.
VITE_API_URL=http://localhost:4000/api
